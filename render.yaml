services:
  - type: web
    name: retail-demand-forecasting
    env: python
    buildCommand: "pip install -r requirements.txt && python src/generate_dataset.py && python src/train_model.py"
    startCommand: "gunicorn app.app:app"
    envVars:
      - key: SECRET_KEY
        generateValue: true
